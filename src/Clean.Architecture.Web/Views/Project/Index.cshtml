@{
    ViewData["Title"] = "ToDo List";
}
@model Clean.Architecture.Web.ApiModels.ProjectDTO
<h2>Project (MVC View)</h2>

<h3>Name: @Model.Name</h3>

<b>Items:</b>

<ul>
    @foreach (var item in Model.Items)
    {
        <li>@item.Title<br />@item.Description</li>
    }
</ul>
